/*
steps needed for SQL fetch:
create pool
route
    create the express route
controller
    create the controller (req, res)
    hold response
model
    variable to hold the model
    sql request
    return result (the await pool)
*/

// "use server"

// export async function fetch(){

// }